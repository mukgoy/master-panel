import { CustomerListener } from "./customer.listener";
import { UploadListener } from "./upload.listener";
import { MasterUserListener } from "./master-user.listener";

export default [
	MasterUserListener,
	CustomerListener,
	UploadListener
]